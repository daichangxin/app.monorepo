{
    "name": "app.monorepo",
    "version": "1.0.0",
    "private": true,
    "description": "",
    "scripts": {
        "dev": "pnpm -r --parallel dev",
        "build:test": "pnpm --stream -r run build:test",
        "build:prod": "pnpm --stream -r run build:prod",
        "clean": "pnpm -r run clean",
        "test": "echo \"hello test\""
    },
    "engines": {
        "node": ">=16",
        "pnpm": ">=8"
    },
    "keywords": [],
    "author": "daichangxin",
    "license": "MIT",
    "dependencies": {
        "axios": "^1.6.2"
    },
    "devDependencies": {
        "@eds-open/eslint-config-bundle": "^1.1.1",
        "rimraf": "^5.0.5",
        "typescript": "^5.3.2"
    }
}
