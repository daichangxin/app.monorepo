import { Checkbox } from '@eds-open/eds-ui';
import { FC } from 'react';

export const Main: FC = () => {
    return (
        <div>
            BasicAuth
            <Checkbox>Hello</Checkbox>
        </div>
    );
};
